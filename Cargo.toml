[package]
name = "IsoTpEngine"
version = "0.1.0"
edition = "2024"

[dependencies]

[lib]
name = "isotp_engine"
path = "src/lib.rs"
crate-type = ["rlib", "cdylib"]

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
